export interface Category {
  label: string;
  pct: number;
  jobs: number;
  color: string;
}

export interface TopArtisan {
  rank: number;
  initials: string;
  bg: string;
  color: string;
  name: string;
  type: string;
  jobs: number;
  rating: number;
}
